// a function is set of codes that can be reused multiple times
function add(x,y)
{
    x=Number(window.prompt(`Enter a number`));
    y=Number(window.prompt(`Enter a number`));   
    return(x+y);
}
console.log(add());

function sub(x,y)
{
    return(x-y);
}
console.log(sub(5,2));
console.log(sub(10,8));

// i like functions functions are great
